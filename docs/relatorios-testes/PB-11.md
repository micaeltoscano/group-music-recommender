# Relatório de Validação — PB-11

## Identificação
- **Sprint:** 2
- **PB:** PB-11 — Scores base e afinidade
- **Data da Validação:** 2026-07-17 (Retroativo)
- **Avaliador:** Agente Testador (Gemini)

## Escopo validado
Pontuação matemática baseada em `track_affinity`, `artist_affinity`, `genre_affinity`, `popularity` e `novelty`. Geração do group score com média, mínimo, diversidade e cobertura.

## Ambiente
- Local (Pytest backend) - Sem dependências externas.

## Alterações analisadas
- `backend/app/engine/scoring.py`
- `backend/tests/test_pb11_scoring_qa.py` (Adversarial)

## Casos planejados executados
- **CT-PB11-01 a CT-PB11-04**: Testados via `test_pb11_scoring.py`, valores esperados corretos e perfeitamente determinísticos. Cobertura da média/mínimo/diversidade validados.

## Testes adicionais executados
- `test_pb11_qa_missing_album_or_none`: Validando fallback se API retornar dicionário `album` como `None`.
- `test_pb11_qa_popularity_string`: Validando casting se a API devolver `popularity` como string `"80"`.
- `test_pb11_qa_group_score_empty_profiles`: Validando falha ao processar score de grupo sem nenhum perfil.

## Critérios de aceitação
Todos os critérios (1 a 5 do PB-11) verificados rigorosamente.

## Defeitos encontrados
- **DEF-PB11-01 — TypeError e AttributeError em Popularity e Album**
  - **Severidade:** Alta (causaria quebra de geração na sala).
  - **Caso de teste:** `test_pb11_qa_missing_album_or_none`, `test_pb11_qa_popularity_string`.
  - **Ambiente:** Motor `scoring.py`.
  - **Pré-condições:** Chaves `album: None` ou `popularity: "80"` no objeto gerado pelo PB-08.
  - **Resultado observado:** O código quebrava sem proteção (TypeError na divisão e AttributeError no `.get()`).
  - **Status:** **Corrigido**. A implementação de `engine/scoring.py` foi reforçada, fazendo fallback estruturado: se `popularity` for str, faz cast para `float`, engolindo exceções (`ValueError`) e caindo para fallback de 0.5. No caso de `album`, aplica `or {}`. Tudo passando.

## Regressões encontradas
Nenhuma.

## Evidências
- Testes limpos no output do pytest `3 passed in 0.01s`.

## Decisão do QA
**APROVADO**

## Próxima ação
Nenhuma (QA Retroativo concluído).
