# Relatório de Validação — PB-01

## Identificação

- **PB:** PB-01 — Fundação técnica do produto
- **Sprint ativa:** Sprint 1 (Fundação técnica, autenticação e sala utilizável)
- **Papel:** Agente de Teste (QA) — validação independente e adversarial
- **Data da validação:** 2026-07-13
- **Gatilho:** PB-01 declarado "Em teste" no `PLANO_EXECUCAO.md` (implementação em commit `d32f3f4`).

## Escopo validado

Critérios de aceitação 1–4 do PB-01 e casos `CT-PB01-01` a `CT-PB01-06` do `PLANO_TESTES.md`:
base local integrada (frontend Vite, backend FastAPI, PostgreSQL, SQLAlchemy/Alembic), conexão
com o banco, ciclo de migração inicial ida/volta e ausência de segredos versionados.

## Ambiente

| Componente | Situação |
|---|---|
| Python | 3.11.9 (venv em `backend/.venv`) |
| Backend deps | fastapi 0.115.6, SQLAlchemy 2.0.36, alembic 1.14.0, pydantic 2.10.4, psycopg2-binary 2.9.10 |
| Docker | 29.6.1 — Postgres 16-alpine via `docker-compose` (container `vibe_db`, healthy) |
| Node.js / npm | **AUSENTES no host** (bloqueio para o frontend nativo) |

## Alterações analisadas

`git status` mostra apenas reorganização de documentos (`docs/`), `docs/prompts/` e `scripts/`;
**nenhuma alteração de código de produção** pendente. O código do PB-01 está commitado. Não há
alterações não relacionadas misturadas ao PB.

Módulos revisados: `backend/app/main.py`, `backend/app/api/health.py`, `backend/app/config.py`,
`backend/app/db/{models,session,base}.py`, `backend/alembic/versions/0001_initial.py`,
`backend/tests/{conftest,test_health}.py`, `frontend/src/{App.jsx,apiClient.js}`,
`docker-compose.yml`, `.env.example`.

## Casos planejados executados

### CT-PB01-01 — Liveness do backend
- Status: **Aprovado**
- Comando: `pytest` + `curl http://127.0.0.1:8011/health` (backend uvicorn real)
- Resultado observado: `200` `{"status":"ok","app":"Vibe Check API","version":"0.1.0"}`
- Evidência: HTTP 200; 4 testes de saúde passam.

### CT-PB01-02 — Readiness com banco alcançável
- Status: **Aprovado**
- Comando: `curl http://127.0.0.1:8011/health/db` contra Postgres 16 real (Docker)
- Resultado observado: `200` `{"status":"ok","database":"ok"}`
- Evidência: HTTP 200 (evidência mais forte que a do implementador, que usou o TestClient/SQLite).

### CT-PB01-03 — Readiness com banco indisponível (falha controlada)
- Status: **Aprovado**
- Comando: `pytest tests/test_health.py::test_health_db_unavailable`
- Resultado observado: sessão que lança `OperationalError` → `503` `{"status":"error","database":"unavailable"}`
- Evidência: 503 sanitizado, sem stack trace nem detalhe de conexão. Confirmado no código
  (`app/api/health.py`) que o `except SQLAlchemyError` não expõe a exceção.

### CT-PB01-04 — Ciclo de migração ida/volta/ida
- Status: **Aprovado**
- Comando: `alembic upgrade head` → `downgrade base` → `upgrade head` contra Postgres real
- Resultado observado:
  - upgrade → tabela `users` + índice único `ix_users_spotify_id` criados (verificado via `psql \d+ users`);
  - downgrade → `to_regclass('public.users')` = NULL (tabela removida);
  - upgrade → `to_regclass('public.users')` = `users` (recriada).
- Evidência: saídas do Alembic sem erro; inspeção direta do catálogo do Postgres em cada etapa.

### CT-PB01-05 — Ausência de segredos versionados
- Status: **Aprovado (com observação — ver DEF-PB01-02)**
- Comando: `git ls-files --error-unmatch .env`; `git check-ignore .env`; varredura de valores (redigida)
- Resultado observado: `.env` **não rastreado** e **git-ignored**; `.env.example` só com placeholders.
- Evidência: git confirma `.env` fora do versionamento. **Observação:** o `.env` local do host
  contém valores reais (CLIENT_ID/CLIENT_SECRET de ~32 chars) — não versionados, logo o critério
  se mantém, mas ver DEF-PB01-02 sobre nomes de chave divergentes do `.env.example`.

### CT-PB01-06 — Frontend nativo conecta a backend e banco
- Status: **Bloqueado**
- Motivo: Node.js/npm ausentes no host → `npm install && npm run dev` (instrução documentada no
  README §4, `http://localhost:5173`) não pode ser executado.
- O que foi possível: revisão do código (`App.jsx`/`apiClient.js`) — a lógica de status é correta
  (indicadores Backend/Banco derivam de `fetch` real a `/health` e `/health/db`; `res.ok` mapeia
  200→"ok", 503→"erro"). A execução **nativa** e a captura da tela com os três "ok" continuam
  pendentes.
- Evidência: `which node` → ausente; sem `dist/` reproduzível neste ambiente.

## Testes adicionais executados

- **Servidor real (não só TestClient):** subida do backend com uvicorn apontando para o Postgres
  do Docker e verificação de `/`, `/health`, `/health/db` — todos 200. O implementador só havia
  registrado evidência via TestClient/SQLite; a readiness contra Postgres real agora está comprovada.
- **Inspeção do catálogo do banco** após cada passo da migração (`to_regclass`, `\d+ users`,
  `pg_indexes`) para confirmar reversibilidade real e não apenas ausência de erro no Alembic.
- **Varredura de segredos no working tree** (`.env`) com redação de valores nas evidências.

## Critérios de aceitação

| # | Critério | Situação |
|---|---|---|
| 1 | Frontend e backend iniciam conforme instruções documentadas | **Parcial** — backend comprovado; frontend nativo **não comprovado** (Node ausente) |
| 2 | Backend conecta ao PostgreSQL | **Comprovado** (200 em `/health/db` contra Postgres real) |
| 3 | Migração inicial do Alembic executa e reverte sem erro | **Comprovado** (ciclo up/down/up inspecionado no banco) |
| 4 | Configurações sensíveis via variáveis de ambiente não versionadas | **Comprovado** (`.env` ignorado; `.env.example` só placeholders) |

## Defeitos encontrados

### DEF-PB01-01 — Critério 1 não comprovável: frontend nativo não executável (Node ausente)
- Severidade: **Média** (bloqueio de ambiente; não é defeito de código)
- Caso relacionado: CT-PB01-06
- Passos: `cd frontend && npm install && npm run dev` → falha (`node`/`npm` não encontrados no host)
- Esperado: Vite em `http://localhost:5173` com três indicadores "ok"
- Observado: impossível iniciar — toolchain ausente
- Impacto: critério 1 do PB-01 permanece sem evidência; validação do PB fica **impedida**
- Recomendação: instalar Node.js ≥ 18 no host e reexecutar CT-PB01-06 com captura de tela.

### DEF-PB01-02 — Divergência de nomes de variáveis entre `.env` local e `.env.example`
- Severidade: **Baixa** (fora do escopo funcional do PB-01; higiene de configuração)
- Caso relacionado: CT-PB01-05
- Observado: o `.env` local usa chaves `CLIENT_ID`/`CLIENT_SECRET` (com espaço antes do `=`),
  enquanto `.env.example` e `config.py` esperam `SPOTIFY_CLIENT_ID`/`SPOTIFY_CLIENT_SECRET`.
- Impacto: nenhum no PB-01 (essas variáveis são de histórias futuras e opcionais); mas causará
  confusão no PB-02. Não expõe segredo versionado.
- Recomendação: alinhar os nomes no `.env` local aos do `.env.example` antes do PB-02.

## Regressões encontradas

Nenhuma (primeira Sprint; sem Sprints anteriores). Os 4 testes de saúde continuam verdes.

## Evidências

- `pytest`: **4 passed** (`tests/test_health.py`).
- Alembic: `upgrade head` / `downgrade base` / `upgrade head` sem erro; `users` +
  `ix_users_spotify_id` criados/removidos/recriados (inspeção via `psql`).
- API real: `GET /`, `GET /health`, `GET /health/db` → 200 contra Postgres 16 (Docker).
- Segurança: `.env` não rastreado e git-ignored; `.env.example` sem segredos.
- Nenhum token/segredo registrado nesta evidência (valores redigidos).

## Limitações da validação

- Frontend não executado nativamente (Node ausente) — critério 1 sem evidência de tela.
- `vite build` em container Node 20 (relatado pelo implementador) **não** foi reproduzido aqui.
- Integração real com Spotify fora do escopo do PB-01.

## Decisão do QA

Todos os casos executáveis passaram e os critérios 2, 3 e 4 estão comprovados — com evidência
mais forte que a original (servidor real + inspeção do catálogo). Porém o **critério 1 permanece
sem evidência** e o caso obrigatório de prioridade alta **CT-PB01-06 está BLOQUEADO** por ausência
de Node no host. Conforme o protocolo do Agente de Teste (§7: "casos bloqueados não devem ser
registrados como aprovados"; §9), o PB **não pode ser declarado VALIDADO**. Não há defeito de
código que justifique REPROVAÇÃO: a validação está **impedida por evidência insuficiente**.

**PB-01 BLOQUEADO NA VALIDAÇÃO — EVIDÊNCIA INSUFICIENTE**

## Próxima ação

1. Instalar Node.js ≥ 18 no host.
2. `cd frontend && npm install && npm run dev`; abrir `http://localhost:5173` com backend + Postgres
   no ar; confirmar os três indicadores "ok" e capturar a tela.
3. Marcar CT-PB01-06 como Aprovado e o critério 1 como concluído; então o QA reemite o veredito
   (`PB-01 VALIDADO — TODOS OS TESTES OBRIGATÓRIOS PASSARAM`).
4. (Higiene) Alinhar nomes de variáveis do `.env` local ao `.env.example` (DEF-PB01-02).
