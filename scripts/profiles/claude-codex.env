# Perfil: Micael — Codex implementa, Claude valida (dois modelos = QA independente de verdade).
# Uso: ./scripts/orquestrar.sh --profile claude-codex ...
CODEX_CMD="codex exec"   # fase de implementação
CLAUDE_CMD="claude -p"   # fase de QA
